class Solution {
    public void reverseString(char[] s) {
        int start = 0;
        int end = s.length - 1;
        char ch =0;
        while(start <= end)
        {
            ch = s[start];
            s[start]=s[end];
            s[end]=ch;
            start++;
            end--;
        }

    }
}
